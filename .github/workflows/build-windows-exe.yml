name: Build Consolidated Two-Room Windows EXE

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
    paths:
      - "school-game/**"
      - "scripts/prepare-school-game-assets.js"
      - "scripts/prepare-consolidated-v49.js"
      - "scripts/school-game-static-test.js"
      - "scripts/prepare-desktop-assets.js"
      - "desktop/assets/**"
      - "package.json"
      - ".github/workflows/build-windows-exe.yml"
  push:
    branches: [main]
    paths:
      - "school-game/**"
      - "scripts/prepare-school-game-assets.js"
      - "scripts/prepare-consolidated-v49.js"
      - "scripts/school-game-static-test.js"
      - "scripts/prepare-desktop-assets.js"
      - "desktop/assets/**"
      - "package.json"
      - ".github/workflows/build-windows-exe.yml"

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    timeout-minutes: 35
    env:
      CSC_IDENTITY_AUTO_DISCOVERY: "false"
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Use Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install dependencies
        shell: pwsh
        run: npm install --no-audit --no-fund

      - name: Validate consolidated two-room game
        shell: pwsh
        run: |
          npm test 2>&1 | Tee-Object -FilePath consolidated-validation.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Build installer and portable EXE
        shell: pwsh
        run: |
          npm run dist:win 2>&1 | Tee-Object -FilePath consolidated-build.log
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Launch packaged consolidated application smoke test
        shell: pwsh
        run: |
          $ErrorActionPreference = "Stop"
          $exe = Get-ChildItem -Path "dist/school-windows/win-unpacked" -Filter "Geometry Tactical Consolidated Local.exe" -File | Select-Object -First 1
          if (-not $exe) { throw "Packaged consolidated application executable was not found." }

          $ready = Join-Path $env:APPDATA "Geometry Tactical Consolidated Local\ready.json"
          Remove-Item $ready -Force -ErrorAction SilentlyContinue
          $process = Start-Process -FilePath $exe.FullName -PassThru
          "Started PID=$($process.Id)" | Tee-Object -FilePath consolidated-startup.log

          for ($attempt = 0; $attempt -lt 45; $attempt++) {
            Start-Sleep -Seconds 1
            $process.Refresh()
            if ($process.HasExited) { throw "Application exited with code $($process.ExitCode)." }
            if (Test-Path $ready) { break }
          }

          if (-not (Test-Path $ready)) {
            & taskkill.exe /PID $process.Id /T /F | Out-Null
            throw "The consolidated game did not report renderer and asset readiness within 45 seconds."
          }

          $readyJson = Get-Content $ready -Raw | ConvertFrom-Json
          if ($readyJson.version -ne "49.0.0") { throw "Unexpected readiness version: $($readyJson.version)" }
          if ($readyJson.edition -ne "consolidated-two-room-v49") { throw "Unexpected readiness edition: $($readyJson.edition)" }
          Get-Content $ready | Tee-Object -FilePath consolidated-startup.log -Append
          "Packaged consolidated game reached the three-role registration screen with the generated V49 runtime and pixel assets loaded." | Tee-Object -FilePath consolidated-startup.log -Append
          & taskkill.exe /PID $process.Id /T /F | Out-Null

      - name: Create SHA-256 checksums
        shell: pwsh
        run: |
          Get-ChildItem -Path dist/school-windows -Filter *.exe | ForEach-Object {
            $hash = (Get-FileHash $_.FullName -Algorithm SHA256).Hash.ToLower()
            "$hash  $($_.Name)" | Out-File -FilePath dist/school-windows/SHA256SUMS.txt -Append -Encoding utf8
          }

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: consolidated-two-room-diagnostics
          path: |
            consolidated-validation.log
            consolidated-build.log
            consolidated-startup.log
          if-no-files-found: warn
          retention-days: 7

      - name: Upload Windows package
        uses: actions/upload-artifact@v4
        with:
          name: geometry-tactical-consolidated-two-room-windows
          path: |
            dist/school-windows/*.exe
            dist/school-windows/SHA256SUMS.txt
          if-no-files-found: error
          retention-days: 30
