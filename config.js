window.TRIAD_CONFIG = Object.freeze({
  // Replace this after deploying server/ to Render, Railway, Fly.io, or another Node host.
  // Example: "wss://triad-territory-server.onrender.com"
  serverUrl: ""
});
