{
  "name": "triad-territory-rush-online",
  "version": "22.0.0",
  "private": true,
  "description": "Teacher-controlled GitHub Pages client plus authoritative Node/WebSocket backend for nine real classroom players.",
  "scripts": {
    "test": "node --check student-v2.js && node --check master.js && node --check config.js && node --check server/server-v2.js && node scripts/smoke-test.js"
  },
  "engines": { "node": ">=20" }
}
