{
  "name": "triad-territory-rush-online",
  "version": "21.0.0",
  "private": true,
  "description": "GitHub Pages client plus authoritative Node/WebSocket backend for nine real classroom players.",
  "scripts": {
    "test": "node --check online-client.js && node --check config.js && node --check server/server.js && node scripts/smoke-test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
