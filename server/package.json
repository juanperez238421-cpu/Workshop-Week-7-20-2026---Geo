{
  "name": "triad-territory-rush-server",
  "version": "25.0.0",
  "private": true,
  "description": "Secure classroom gateway for nine PC players, three teams and automatic music-based team names.",
  "main": "secure-gateway.js",
  "scripts": {
    "start": "node --require ./runtime-patch.js secure-gateway.js",
    "test": "node --check runtime-patch.js && node --check secure-gateway.js && node --check server-v3.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
