{
  "name": "triad-territory-rush-server",
  "version": "28.0.0",
  "private": true,
  "description": "Secure classroom gateway with geometry respawn questions, reliable mouse aiming, player team selection, human-only kill announcements, three lives, ammunition and supply powers.",
  "main": "secure-gateway.js",
  "scripts": {
    "start": "node --require ./runtime-v8.js secure-gateway.js",
    "test": "node --check runtime-patch.js && node --check runtime-v6.js && node --check runtime-v7.js && node --check runtime-v8.js && node --check secure-gateway.js && node --check server-v3.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
