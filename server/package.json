{
  "name": "triad-territory-rush-server",
  "version": "29.0.0",
  "private": true,
  "description": "Low-latency classroom gateway with 50 Hz simulation, 20 Hz delta snapshots, swept projectile collisions, enlarged hitboxes, automatic ammunition regeneration and geometry respawn questions.",
  "main": "secure-gateway.js",
  "scripts": {
    "start": "node --require ./runtime-v9.js secure-gateway.js",
    "test": "node --check runtime-patch.js && node --check runtime-v6.js && node --check runtime-v7.js && node --check runtime-v8.js && node --check runtime-v9.js && node --check secure-gateway.js && node --check server-v3.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}