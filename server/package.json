{
  "name": "triad-territory-rush-server",
  "version": "26.0.0",
  "private": true,
  "description": "Secure classroom gateway with 40 Hz simulation, 15 Hz compressed snapshots, three lives, five ammo charges, supply boxes and server-side backpressure control.",
  "main": "secure-gateway.js",
  "scripts": {
    "start": "node --require ./runtime-v6.js secure-gateway.js",
    "test": "node --check runtime-patch.js && node --check runtime-v6.js && node --check secure-gateway.js && node --check server-v3.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {
    "express": "^4.21.2",
    "ws": "^8.18.0"
  }
}
